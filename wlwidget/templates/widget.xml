<Module>
 <ModulePrefs title="Hello World!">
 <Require feature="opensocial-0.9" />
 <Require feature="osapi" />
 <Require feature="pubsub" />
 <Require feature="dynamic-height" />
 </ModulePrefs>
 <Content type="html" view="home">
 <![CDATA[
<script type="text/javascript" src="http://open-app.googlecode.com/files/openapp.js"></script>
<script type="text/javascript" src="{{ url_for('static', filename='smartgateway.js', _external = True) }}"></script>
<script type="text/javaScript">

    function processMessages(e) {
        var ifr = document.getElementById('weblabIFrame');
        if(e.origin == 'http://cloud.weblab.deusto.es') {
            e.source.postMessage("I'm your parent", e.origin);
        }
    }

    function init() {
        
        // Register event listener
        window.addEventListener('message', processMessages, false);

        // Adjust height
        gadgets.window.adjustHeight();

        // Create SmartGateway
        var container = document.getElementById('container');
        var sg = new SmartGateway(container, '{{ url }}');
        sg.registerOnLoad( function(reserationId) {
            container.innerHTML = "Got a reservation! " + reservationId;
        });
    }

    gadgets.util.registerOnLoadHandler(init);
</script>
 <div id="container">
 </div>
 ]]>
</Content>
</Module>
